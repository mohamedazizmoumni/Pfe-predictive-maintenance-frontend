export const environment = {
  production: true,
  apiUrl: 'http://localhost:8080/api/v1',
  wsUrl: 'http://localhost:8080',
  // Public marketing site (sentinel-client side) — split into its own
  // deployable project. Placeholder domain — swap for the real one once
  // registered/hosted.
  marketingUrl: 'https://sentinel-maintenance.io',
};
