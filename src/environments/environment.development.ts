export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api/v1',
  wsUrl: 'http://localhost:8080',
  // Public marketing site (sentinel-client side) — separately deployed
  // project, runs on its own dev server port so it doesn't clash with ggg.
  marketingUrl: 'http://localhost:4300',
};
